import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Terms of Use | FilX — x402 File Primitive",
  description: "Terms of Use for FilX.io — the x402 File Primitive for AI agents.",
};

export default function TermsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
