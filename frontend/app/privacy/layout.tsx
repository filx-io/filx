import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Privacy Policy | FilX — x402 File Primitive",
  description: "Privacy Policy for FilX.io — the x402 File Primitive for AI agents.",
};

export default function PrivacyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
